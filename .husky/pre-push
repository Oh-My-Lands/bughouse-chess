set -e

echo "Running pre-push checks..."
echo ""

# 1. Lint and type check
echo "=== Linting ==="
npm run lint

# 2. Unit tests
echo ""
echo "=== Unit Tests ==="
npm run test:unit

# NOTE: Cypress component/E2E tests are temporarily out of the pre-push gate.
# They were built around the Firebase Auth/Firestore emulator, which was
# removed with the rest of Firebase. The specs have been de-Firebased and
# type-check, but the `cypress-with-emulator.sh` runner still boots the
# emulator. Re-enable these once the runner no longer depends on it.

echo ""
echo "All pre-push checks passed!"
